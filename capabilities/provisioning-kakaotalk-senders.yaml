naftiko: 1.0.0-alpha2
info:
  label: Sinch Provisioning API — KakaoTalk Senders
  description: 'Sinch Provisioning API — KakaoTalk Senders. 2 operations. Lead operation: List Kakaotalk Senders. Self-contained
    Naftiko capability covering one Sinch business surface.'
  tags:
  - Sinch
  - KakaoTalk Senders
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SINCH_API_KEY: SINCH_API_KEY
capability:
  consumes:
  - type: http
    namespace: provisioning-kakaotalk-senders
    baseUri: https://provisioning.api.sinch.com
    description: Sinch Provisioning API — KakaoTalk Senders business capability. Self-contained, no shared references.
    resources:
    - name: v1-projects-project_id-kakaoTalk-senders
      path: /v1/projects/{project_id}/kakaoTalk/senders
      operations:
      - name: listkakaotalksenders
        method: GET
        description: List Kakaotalk Senders
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createkakaotalksender
        method: POST
        description: Create a Kakaotalk Sender
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SINCH_API_KEY}}'
  exposes:
  - type: rest
    namespace: provisioning-kakaotalk-senders-rest
    port: 8080
    description: REST adapter for Sinch Provisioning API — KakaoTalk Senders. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v1/projects/{project-id}/kakaotalk/senders
      name: v1-projects-project-id-kakaotalk-senders
      description: REST surface for v1-projects-project_id-kakaoTalk-senders.
      operations:
      - method: GET
        name: listkakaotalksenders
        description: List Kakaotalk Senders
        call: provisioning-kakaotalk-senders.listkakaotalksenders
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createkakaotalksender
        description: Create a Kakaotalk Sender
        call: provisioning-kakaotalk-senders.createkakaotalksender
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: provisioning-kakaotalk-senders-mcp
    port: 9090
    transport: http
    description: MCP adapter for Sinch Provisioning API — KakaoTalk Senders. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: list-kakaotalk-senders
      description: List Kakaotalk Senders
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: provisioning-kakaotalk-senders.listkakaotalksenders
      outputParameters:
      - type: object
        mapping: $.
    - name: create-kakaotalk-sender
      description: Create a Kakaotalk Sender
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: provisioning-kakaotalk-senders.createkakaotalksender
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
